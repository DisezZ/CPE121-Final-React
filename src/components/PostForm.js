import React from "react";
import {
    Grid
} from '@material-ui/core'

export default class PostForm extends React.Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return (
        <Grid>
            <div>
                Post Form
            </div>
        </Grid>
    );
  }
}
